import 'package:flutter/material.dart';

class BeforeDashboard extends StatelessWidget {
  const BeforeDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Enable internet"),
      ),
    );
  }
}
